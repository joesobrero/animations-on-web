import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["animations-on-the-web-git-how-i-use-3066e1-emilkowalski-s-team.vercel.app"],
  },
};

export default nextConfig;
